info:
  name: Get types
  type: http
  seq: 2

http:
  method: GET
  url: https://pokeapi.co/api/v2/type?limit=30
  params:
    - name: limit
      value: "30"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
